
// #include "Time.h"
// #include <iostream>
// #include "States.h"
// #include "FSM.h"
#include "Action.h"
using namespace std;



int main()
{
	// State red("Red");
	// State yellow("Yellow");
	// State green("Green");

	// Port reset;
	// Port increment;
	// Port idle;

	// Condition lessThan10(
	// 	function<bool()>([] { return Time < 10; })
	// );

	// Condition lessThan5(
	// 	function<bool()>([] { return Time < 5; })
	// );

	// Condition equals10(
	// 	function<bool()>([] { return Time == 10; })
	// );

	// Condition equals5(
	// 	function<bool()>([] { return Time == 5; })
	// );

	// Action incrementT(
	// 	function<void()>([] { Time += 1; })
	// );

	// Action resetT(
	// 	function<void()>([] { Time = 0; })
	// );

	// Transition t1(red, red, increment, lessThan10, incrementT);
	// Transition t2(red, green, reset, equals10, resetT);

	// Transition t3(green, green, increment, lessThan10, incrementT);
	// Transition t4(green, yellow, reset, equals10, resetT);

	// Transition t5(yellow, yellow, increment, lessThan5, incrementT);
	// Transition t6(yellow, red, reset, equals5, resetT);

	// States Q = red + green + yellow;
	// Transitions T = t1 + t2 + t3 + t4 + t5 + t6;

	// FSM m(Q, &red, T);

	// m.printStates();
	// m.drawFSM();

	// m.run(60);
	
	// m.reset(&red);

	// m.run(45);

	// system("pause");
}